// ═══════════════════════════════════════
//  GHOST PROFILE — CONFIG
//  Only change DISPLAY_NAME here.
//  API keys go in Vercel Environment Variables.
// ═══════════════════════════════════════
const CONFIG = {
  DISPLAY_NAME: "GHOST",      // ← Change this to your gamer name
  RIOT_API_KEY:  "",          // ← Leave blank — add in Vercel env vars
  RIOT_PUUID:    "",          // ← Leave blank — add in Vercel env vars
  RIOT_SUMMONER_ID: "",       // ← Leave blank — add in Vercel env vars
  RIOT_REGION:   "na1",      // ← Change to your region: na1, euw1, ap, kr
  VALORANT_REGION: "na",     // ← Change to your region: na, eu, ap, kr
};
